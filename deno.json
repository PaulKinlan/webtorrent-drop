{
  "tasks": {
    "dev": "deno run --unstable-kv --allow-net --allow-read --allow-env server.ts",
    "start": "deno run --unstable-kv --allow-net --allow-read --allow-env server.ts",
    "check": "deno check server.ts telemetry.ts",
    "fmt": "deno fmt"
  },
  "fmt": {
    "lineWidth": 100,
    "exclude": ["static/vendor/"]
  },
  "lint": {
    "exclude": ["static/vendor/"]
  },
  "deploy": {
    "org": "paulkinlan-ea",
    "app": "webtorrent-drop"
  }
}
